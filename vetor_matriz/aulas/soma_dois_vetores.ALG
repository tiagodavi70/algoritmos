Algoritmo "soma_vetores"

Var
a, b, c: vetor[0..4] de inteiro
i: inteiro

Inicio

para i de 0 ate 4 faca
   leia(a[i])
   leia(b[i])
fimpara

para i de 0 ate 4 faca
   c[i] <- a[i] + b[i]
fimpara

para i de 0 ate 4 faca
   escreva(c[i])
fimpara

Fimalgoritmo