Algoritmo "soma_ate10"
// Faça um algoritmo que calcule e imprime a soma dos inteiros de 1 a 10.
// Utilize a estrutura ENQUANTO para fazer um laço com as instruções de
// cálculo e incremento.
// O laço deve terminar quando o valor de x se tornar 11.

Var
i, soma: inteiro

Inicio

i <- 0
soma <- 0

enquanto i < 11 faca
   soma <- soma + i
   i <- i + 1
fimenquanto

escreva(soma)

Fimalgoritmo